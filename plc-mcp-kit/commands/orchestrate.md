---
description: Legacy slash-entry shim for dmux-workflows and autonomous-agent-harness. Prefer the skills directly.
---

# Orchestrate Command (Legacy Shim)

This command has been migrated to the following skills:

- `dmux-workflows` — for parallel panes, worktrees, and multi-agent splits
- `autonomous-agent-harness` — for longer-running loops, governance, scheduling, and control-plane execution

Prefer invoking those skills directly instead of this shim.
