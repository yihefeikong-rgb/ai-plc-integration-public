# 前端全量复审报告 — 2026-07-20

> **复审人**：code-reviewer agent（独立于实现者）
> **复审范围**：`ai-plc-assistant/frontend/`
> **复审基线**：master @ `9c71967`
> **复审日期**：2026-07-20
> **评分**：6.8 / 10 — **CONDITIONAL PASS**（需修复后再合并生产）
> **本次为 Batch 8/9 推迟的统一复审**

---

## 一、findings.md 清单核对（11 项）

核对 `findings.md` 顶部"仍待修复"清单是否仍然存在：

| 编号 | 状态 | 证据 |
|------|------|------|
| F-019 机器人 4 模式 | **仍存在** | `src/components/RobotPanel.jsx:30` 仅 `INITIAL_STATE.backend='simulated'` 一个模式，无 mode 切换；全文未出现"演示/仿真/只读/真实控制"或 `safetyLevel`/`level`/`mode` 逻辑 |
| F-037 useTabs closeTab updater 内副作用 | **仍存在** | `src/hooks/useTabs.js:28-38` `closeTab` 在 `setTabs(prev => { ... setActiveTab(cur => ...) ; return next })` 内调用 `setActiveTab`，仍是 updater 内触发副作用反模式 |
| ToolStatusBar 接入 5 个工具页面 | **仍存在** | `ToolStatusBar` 仅在 `ui/ToolStatusBar.jsx`、`ui/index.js`、`ui/ui.test.jsx` 三处引用；5 个工具页（LadderGenerator/CodeExplainer/IoTableGenerator/FaultDiagnosis/VariableAnalyzer）全部未 import |
| 5 个工具页面状态机完整改造 | **仍存在** | 5 个工具页均仅 `const [loading, setLoading] = useState(false)` 单状态：`LadderGenerator.jsx:30`、`CodeExplainer.jsx:41`、`IoTableGenerator.jsx:39`、`FaultDiagnosis.jsx:43`、`VariableAnalyzer.jsx:37`；未实现 10 种状态 |
| 附件上传按钮 onClick | **仍存在** | `src/layout/AppShell.jsx:214` `onAddAttachment={() => addLog('info', '[附件] 附件上传功能待接入')}`；`ChatArea.jsx:588-595` 附件按钮接此空函数，无 `<input type="file">` |
| layout/ 单元测试 | **仍存在** | `find src/layout -name "*.test.*"` 无结果；9 个 layout 组件全部无单元测试 |
| 响应式 4 尺寸截图回归 | **仍存在** | `scripts/` 下只有 `capture-baseline.mjs` + `verify-batch2~7.mjs`，无 1366/1600/1920/2560 四尺寸截图回归脚本 |
| E2E 测试 | **仍存在** | `find -name "playwright*" -o -name "*.spec.*"` 无结果；`package.json` 无 `test:e2e` 脚本；仅安装了 `@playwright/test` 但未配置 `playwright.config` |
| Lighthouse 性能测试 | **仍存在** | 无 lighthouse 脚本/配置；`package.json` 无 `lighthouse` 脚本 |
| CSP 收紧 connect-src | **仍存在** | `index.html:12` 仍是 `connect-src 'self' http://localhost:* http://127.0.0.1:* https:` — `https:` 通配未收紧到具体域名 |
| Batch 8/9 独立复审 | **本次完成** | `progress.md:160` Batch 8 简化交付、`progress.md:123` Batch 9 简化交付；**本次复审即为该统一复审** |

**结论**：原 11 项中 10 项仍存在，1 项（Batch 8/9 复审）本次完成。

---

## 二、新发现问题（47 条，F-042 ~ F-090）

> 按 HIGH / MEDIUM / LOW 严重级别分组。F-062 与 F-037 合并看待；F-090 与 F-043 合并。

### HIGH（6 项 — 阻断生产部署）

#### F-050: OrchestratorInspector 硬编码"状态：运行中"伪造后端状态
- **位置**：`src/layout/InspectorPanel.jsx:383-411`
- **现象**：
  ```jsx
  <KeyValue k="服务器" v="orchestrator" />
  <KeyValue k="协议" v="MCP stdio" />
  <KeyValue k="状态" v="运行中" />   // 硬编码"运行中"
  <div>· Team Lead（调度）</div>      // 硬编码 Agent 列表
  <div>· Developer（实现）</div>
  ```
- **影响**：用户在 Inspector 看到"状态：运行中"是假数据，实际后端可能未启动。`GlobalStatusBar` 已接 `orchestratorHealth()` 真实状态，这里却硬编码，自相矛盾。**违反项目"不伪造"安全原则**。
- **分类**：收尾批次 D-3 引入
- **严重级别**：HIGH
- **修复方向**：接 `orchestratorHealth()` 真实状态，未启动时显示"未连接"

#### F-051: IoTableInspector 地址范围与校验全部硬编码假数据
- **位置**：`src/layout/InspectorPanel.jsx:290-308`
- **现象**：
  ```jsx
  <div><span className="text-accent font-mono">I0.0 ~ I0.7</span> 输入区</div>
  ...
  <div>· 地址冲突检测</div>
  <div>· 重复分配检测</div>
  <div>· 类型匹配校验</div>
  ```
  无论 `currentProject` 是什么 PLC 型号、无论 IO 表实际生成结果，永远显示固定的 I0.0~I0.7 / Q0.0~Q0.7 / M0.0~M14.7 范围与"地址冲突检测"等静态文案。
- **影响**：用户以为系统已做校验，实际未做。**工业场景下"假校验通过"比"未校验"更危险**。
- **分类**：收尾批次 D-3 引入
- **严重级别**：HIGH
- **修复方向**：明确标注"示例地址范围"或"校验功能待接入"，不显示确定性文案

#### F-067: Dashboard 全局状态 4 项硬编码与 GlobalStatusBar 矛盾
- **位置**：`src/components/Dashboard.jsx:128-141`
- **现象**：
  ```jsx
  <StatusRow label="PLC" value="未连接" tone="offline" />
  <StatusRow label="TIA Portal" value="未启动" tone="offline" />
  <StatusRow label="PLCSIM" value="未启用" tone="neutral" />
  <StatusRow label="安全模式" value="只读" tone="readonly" />
  ```
  无论后端真实状态如何，永远显示这 4 个值。`GlobalStatusBar` 已接 `listServers()`/`healthCheck()`/`orchestratorHealth()` 真实状态，Dashboard 却硬编码。
- **影响**：用户在 Dashboard 看到的"全局状态"与 GlobalStatusBar 看到的不一致。GlobalStatusBar 显示"PLC 已连接"，Dashboard 显示"PLC 未连接"，用户困惑。
- **分类**：Batch 5 重构遗留（`progress.md:271` 也提到"打桩"）
- **严重级别**：HIGH
- **修复方向**：Dashboard 接 `listServers()`/`healthCheck()`/`orchestratorHealth()`，或直接读 GlobalStatusBar 已有的状态

#### F-068: Dashboard 安全模式硬编码"只读"与 GlobalStatusBar localStorage 脱节
- **位置**：`src/components/Dashboard.jsx:141`
- **现象**：`<StatusRow label="安全模式" value="只读" tone="readonly" />` — 永远显示"只读"。GlobalStatusBar 的安全等级切换会写 `localStorage['ai-plc:safety-level']`，Dashboard 不读取该 key。
- **影响**：用户在 GlobalStatusBar 切到"L3 设备控制"后，切到 Dashboard 仍看到"只读"，状态不同步。A-3 修复 F-018 时未同步到 Dashboard。**安全边界误判**。
- **分类**：收尾批次 A-3 修复不完整
- **严重级别**：HIGH
- **修复方向**：读 `localStorage['ai-plc:safety-level']` 或共享 state

#### F-042: useConversation 非流式 fallback fetch 缺失 localControlHeaders
- **位置**：`src/hooks/useConversation.js:211-216`
- **现象**：SSE 失败回退到非流式 `/chat` 时：
  ```js
  const res = await fetch(`${API_BASE}/chat`, {
    method: 'POST',
    headers: { 'Content-Type': 'application/json' },  // 缺 localControlHeaders()
    body: JSON.stringify({ model_id: selectedModel, ... }),
    signal: controller.signal,
  })
  ```
  而 `api.js:145` 的 `streamChat` 与 `api.js:25` 的 `request()` 都会注入 `localControlHeaders()`。
- **影响**：本地 API Token 鉴权开启时（后端要求 `X-Local-Api-Token`），SSE 失败后非流式 fallback 会因缺 Token 被后端拒绝，导致用户看到的不是"主模型不可用 → 切到备用"，而是"403 Forbidden"。与流式主路径行为不一致。
- **分类**：原有问题（非本次收尾批次引入，从早期版本延续）
- **严重级别**：HIGH
- **修复方向**：`headers: { 'Content-Type': 'application/json', ...localControlHeaders() }`

#### F-056: SettingsPanel testResult.reply 渲染模型回复到 UI（F-024 残留）
- **位置**：`src/components/SettingsPanel.jsx:69`
- **现象**：`{testResult.reply && <span className="text-2xs opacity-70">"{testResult.reply}"</span>}` — 直接渲染后端返回的 `reply` 字段（模型回复内容）。F-024 已标注"reply 可能包含敏感信息，出现在截图"，至今未修。
- **影响**：测试连接的模型回复（可能包含 API Key 片段、内部提示等）直接显示在 UI，截图分享时泄露。
- **分类**：原有问题（F-024 残留）
- **严重级别**：HIGH
- **修复方向**：仅 dev 模式显示，或显示前脱敏

### MEDIUM（16 项 — 建议修复）

#### F-043: 5 个工具页面 streamChat 调用未传 signal，无法中止
- **位置**：
  - `src/components/CodeExplainer.jsx:56` `await streamChat({ model_id: selectedModel, messages, temperature: 0.2, onToken, onDone, onError })`
  - `src/components/IoTableGenerator.jsx:51` 同上
  - `src/components/FaultDiagnosis.jsx:58` 同上
  - `src/components/VariableAnalyzer.jsx:48` 同上
  - `src/components/LadderGenerator.jsx:46` `generateLadder(description, {}, '', selectedModel)` — 未传 signal
- **现象**：4 个 streamChat 调用均未传 `signal` 参数，`LadderGenerator` 也未传 signal 给 `generateLadder`。
- **影响**：用户切走 tab 后这些工具页的请求仍在后台进行；`MainWorkspace.jsx:82-92` 用 `display:none` 保持挂载，组件不卸载，无法通过 `useEffect` cleanup 中止。Token 持续消耗，UI 状态可能被异步 `setResult` 写入已切走的 tab。Batch 6 给 ChatArea 加了 AbortController，但 5 个工具页未同步。
- **分类**：原有问题（Batch 6 修了 ChatArea 路径，工具页遗漏）
- **严重级别**：MEDIUM

#### F-047: OrchestratorPanel 文件 870 行超 800 行阈值
- **位置**：`src/components/OrchestratorPanel.jsx`（870 行）
- **现象**：单文件包含主组件 + `StatCard`/`StepResultRow`/`ToolGroup`/`RunDialog`/`TutorialModal`/`Section`/`ToolCard`/`Step` 8 个子组件 + 3 张中文映射表 + `apiGet/apiPost/apiDelete` helper + `formatUptime`。`findings.md` F-006 已标注"Batch 7/8 拆分"，但 Batch 7/8/9 与收尾批次均未拆分。
- **影响**：超过 CLAUDE.md 项目规则中 800 行阈值；可维护性差；状态机散落（`useState` 共 13 个）。
- **分类**：原有问题（F-006 残留）
- **严重级别**：MEDIUM

#### F-052: DiagnoseInspector 设备状态显示"待接入"但排查步骤静态硬编码
- **位置**：`src/layout/InspectorPanel.jsx:365-378`
- **现象**：排查步骤 1-5 全部硬编码字符串（"检查 PLC 电源与连接"等），未根据 `messages` 中的 `warning`/`error` 内容生成针对性排查建议；"设备状态"标注"待接入 PLC 状态 API"。
- **影响**：用户看到通用排查步骤会误以为是针对当前故障的诊断。误导性。
- **分类**：收尾批次 D-3 引入
- **严重级别**：MEDIUM

#### F-053: VariablesInspector 命名规范与地址分配硬编码
- **位置**：`src/layout/InspectorPanel.jsx:440-455`
- **现象**：硬编码"bXxx — Bool/iXxx — Int/rXxx — Real"命名规范 + "M0.0~M14.7 标志位/MW20~MW40 Int/MD60~MD100 Real"地址范围，与实际 `lastVars` 数据无关。
- **影响**：与 F-051 同类问题，假数据误导。
- **分类**：收尾批次 D-3 引入
- **严重级别**：MEDIUM

#### F-055: ErrorBoundary componentDidCatch console.error 不脱敏
- **位置**：`src/components/ErrorBoundary.jsx:15`
- **现象**：`console.error('[ErrorBoundary]', error, info.componentStack)` 把错误对象与组件栈完整打到控制台。
- **影响**：生产环境可能泄露内部路径/敏感 props（如 API Key 在 form state 中传入 SettingsPanel，渲染崩溃时 stack 可能包含 form 值）。CLAUDE.md 安全原则要求"禁止将密钥、令牌、密码或本地环境文件内容写入代码、日志、报告或 Git"。控制台日志也属于日志。
- **分类**：原有问题
- **严重级别**：MEDIUM

#### F-057: SettingsPanel handleTest 先调 updateSettings(form) 静默失败
- **位置**：`src/components/SettingsPanel.jsx:155`
- **现象**：`try { await updateSettings(form) } catch {}` — 测试连接前先保存设置，但 catch 完全静默吞错。
- **影响**：用户点了"测试连接"，如果保存失败（如后端 422 校验错误），用户无感知，测试连接会以旧配置运行，得到误导性结果。
- **分类**：原有问题
- **严重级别**：MEDIUM

#### F-058: OrchestratorPanel 教程弹窗与 RunDialog 未走 useFocusTrap/useEscClose
- **位置**：`src/components/OrchestratorPanel.jsx:688-720`（`RunDialog`）、`OrchestratorPanel.jsx:735-837`（`TutorialModal`）、`AppShell.jsx:277-302`（About 弹窗）
- **现象**：F-015 收尾批次给 `ConfirmDialog` + 3 个 TemplateModal + `CreateProjectDialog` 加了 `useFocusTrap`，但 `RunDialog`、`TutorialModal`、`About` 三个弹窗未加；`useEscClose` 也未应用。
- **影响**：可访问性不一致；Tab 键可跳到弹窗外背景元素；Esc 不能关。Batch 8 F-016 的修复目标"6 个弹窗"实际只覆盖 5 个。
- **分类**：收尾批次遗漏
- **严重级别**：MEDIUM

#### F-064: PromptTemplateModal 变量替换用 String.replace 不防转义
- **位置**：`src/components/PromptTemplateModal.jsx:46-49`
- **现象**：
  ```js
  let content = selected.content
  Object.entries(varValues).forEach(([k, v]) => {
    content = content.replace(`{${k}}`, v)
  })
  ```
  - 用 `String.replace` 只替换第一个匹配，模板中如果同一变量出现两次，第二次不会被替换。
  - 用户输入 `v` 若含 `$&`/`$$` 等 `String.replace` 特殊模式字符，会被错误解释（如输入 `"$&"` 会被替换为匹配内容本身）。
- **影响**：模板变量替换不完整/不安全；工业 prompt 中变量重复出现常见（如 `{plc_type}` 在功能概述与代码示例中都出现）。
- **分类**：原有问题
- **严重级别**：MEDIUM

#### F-066: PrimarySidebar 删除对话用 inline 二次确认而非 ConfirmDialog
- **位置**：`src/layout/PrimarySidebar.jsx:285-312`
- **现象**：删除对话按钮点击后，inline 显示"确认删除对话 / 取消"两个按钮，未用已存在的 `ui/ConfirmDialog`（支持 `dangerAction`/焦点锁定/Esc 关闭）。
- **影响**：A-2 修复 F-017 时仅在 ConfirmDialog 内加了 `dangerAction` 支持，但 PrimarySidebar 删除对话未实际调用 ConfirmDialog，而是自己造了一套 inline 确认。Focus trap/Esc/A11y 全无。F-017 修复不完整。
- **分类**：收尾批次 A-2 修复不完整
- **严重级别**：MEDIUM

#### F-069: 无 Zod/Schema 校验，API 响应直接解构使用
- **位置**：全局，如 `src/hooks/useConversation.js:62-68`、`src/layout/InspectorPanel.jsx:267-273`
- **现象**：所有 API 响应直接 `d.conversations || []`、`data.results || []` 解构，无 schema 校验。`parseContent` 用 `try { JSON.parse } catch {}` 容错但返回 `{}`。
- **影响**：后端返回字段变更（如 `conversations` 改名 `items`）时，前端静默失败显示空状态，无显式错误。违反 CLAUDE.md 输入验证原则"始终在系统边界验证"。
- **分类**：原有问题
- **严重级别**：MEDIUM

#### F-070: 9 个 catch {} 完全静默吞错
- **位置**：含 `api.js:180`、`AppShell.jsx:46/50`、`useConversation.js:30`、`GlobalStatusBar.jsx:35/112`、`useWorkbenchHistory.js:23/31/38`、`PrimarySidebar.jsx:157`、`SettingsPanel.jsx:155`
- **现象**：9 处 `catch {}` 无任何错误处理或日志。
- **影响**：违反 CLAUDE.md 错误处理原则"永远不要静默吞掉错误"。localStorage 写失败（如隐私模式/配额满）时用户无感知；`api.js:180` SSE JSON.parse 失败时静默丢消息。
- **分类**：原有问题
- **严重级别**：MEDIUM

#### F-075: ChatArea CitationMessage 用 `window.open(url, '_blank')` 但未加 `rel="noopener noreferrer"`
- **位置**：`src/components/ChatArea.jsx:393-398`
- **现象**：
  ```js
  const handleDownload = () => {
    if (url) {
      window.open(url, '_blank')   // 无 noopener
    } else if (fileContent) { ... }
  }
  ```
  而 CitationMessage 的 `<a>` 标签（`ChatArea.jsx:445`）正确加了 `rel="noopener noreferrer"`。
- **影响**：`window.open` 未加 noopener 时，新窗口可通过 `window.opener` 访问原窗口，存在反向 tabnabbing 风险（若 url 是外部不可控链接）。
- **分类**：收尾批次 D-1 引入
- **严重级别**：MEDIUM

#### F-078: ChatArea useEffect scrollIntoView 在 messages 变化时强制滚动，打断用户回看
- **位置**：`src/components/ChatArea.jsx:660-662`
- **现象**：
  ```js
  useEffect(() => {
    endRef.current?.scrollIntoView({ behavior: 'smooth' })
  }, [messages])
  ```
  每次 `messages` 变化（包括 streaming 中每个 token 到达导致的 setMessages）都强制滚动到底。
- **影响**：streaming 中用户想往上滚动回看时，每个 token 到达都会把视图拉回底部，无法回看。`showScrollBtn` 状态有检测 `atBottom`，但 useEffect 不判断 atBottom，与 `handleScroll` 状态不同步。
- **分类**：Batch 6 引入
- **严重级别**：MEDIUM

#### F-080: GlobalStatusBar PLC/TIA/PLCSIM 用服务器名字正则推断，非真实状态
- **位置**：`src/layout/GlobalStatusBar.jsx:40-53`
- **现象**：`matchServers` 用 `/plcsim|plc.sim/i`、`/tia/i`、`/plc|s7/i` 正则匹配服务器名，把 `status === 'connected' || 'running' || 'ok'` 当作真实连接。
- **影响**：服务器命名规范变化时（如 `plc-mcp-bridge` 重命名为 `s7-bridge`），状态推断会错。D-4 宣称"接入真实 API"，但实际是"按名字猜"，不是真实 PLC 连接状态。需后端提供 `plc_connected`/`tia_running` 等显式字段。
- **分类**：收尾批次 D-4 实现方式
- **严重级别**：MEDIUM

#### F-086: CSP `script-src 'unsafe-inline'` 未迁移到 nonce-based
- **位置**：`index.html:12`
- **现象**：`script-src 'self' 'unsafe-inline'` — 注释明确"Vite dev 模式需要内联脚本，生产构建后可改用 nonce-based CSP"，但生产构建后未改。
- **影响**：`'unsafe-inline'` 削弱 CSP 防 XSS 能力；Vite 生产构建后无内联脚本，可收紧。
- **分类**：F-022 Batch 2 已放宽，但收紧未完成
- **严重级别**：MEDIUM

#### F-087: useConversation handleSend 依赖 messages 导致每次消息变化重建
- **位置**：`src/hooks/useConversation.js:258`
- **现象**：`handleSend` 的 `useCallback` 依赖数组 `[sending, openTab, addLog, selectedModel, messages, ensureConversation, currentProject]` — 包含 `messages`。每次消息变化（streaming 中每个 token）`handleSend` 重新创建。
- **影响**：`ChatArea` 接收 `onSend={handleSend}`，handleSend 引用变化触发 ChatArea 重渲染。streaming 中 ChatArea 重渲染频繁，可能卡顿。应改用 `setMessages(prev => ...)` 内访问最新 messages，从依赖中移除 `messages`。
- **分类**：Batch 6 引入
- **严重级别**：MEDIUM

#### F-062: useTabs closeTab 在 StrictMode 双调用下可能 setActiveTab 两次（与 F-037 合并看待）
- **位置**：`src/hooks/useTabs.js:28-38`
- **现象**：F-037 已记录的 updater 内副作用反模式，叠加 `main.jsx:7` 的 `<React.StrictMode>`。React 18 StrictMode 在开发模式下会双调用 reducer/updater，`setActiveTab` 会被调用两次。
- **影响**：当前 `setActiveTab(cur => cur !== id ? cur : next[next.length - 1]?.id || 'welcome')` 是幂等的，第二次调用结果与第一次相同，无实际 bug。但仍是反模式，React 19 严格模式可能告警。
- **分类**：F-037 子项，叠加 StrictMode
- **严重级别**：MEDIUM（与 F-037 合并看待）

#### F-090: 4 个工具页 streamChat 调用未传 abort signal（与 F-043 合并）
- **位置**：见 F-043
- **现象**：F-043 子项，强调 streamChat 的 `signal` 参数未传，4 个工具页无停止按钮，用户无法中止生成。
- **影响**：与 F-043 同。
- **分类**：F-043 衍生
- **严重级别**：MEDIUM（合并到 F-043）

### LOW（25 项 — 可选修复）

#### F-044: OrchestratorPanel 直接 fetch 三处绕过 api.js request 封装
- **位置**：`src/components/OrchestratorPanel.jsx:101-117`
- **现象**：自封装 `apiGet`/`apiPost`/`apiDelete` 三个 helper 直接 fetch，虽然已通过 `localControlHeaders()` 注入 Token，但仍绕过 `api.js:20` 的 `request()`，未走统一的错误处理与 URL 拼接逻辑。F-020 已记录但仅标注"后续 Batch 修复"，至今未修。
- **影响**：维护成本高；若 `request()` 增加重试/拦截/超时，OrchestratorPanel 不受益。
- **分类**：原有问题
- **严重级别**：LOW

#### F-045: RobotPanel 直接 fetch 第四处绕过 api.js 且重复定义 LOCAL_API_TOKEN
- **位置**：`src/components/RobotPanel.jsx:24` + `RobotPanel.jsx:132-139`
- **现象**：
  ```js
  const LOCAL_API_TOKEN = import.meta.env.VITE_LOCAL_API_TOKEN  // 第 24 行，与 api.js:14 重复定义
  ...
  const res = await fetch(`${API_BASE}/orchestrator/workflows/robot_pick_place/run`, { ... })
  ```
  `api.js` 已有 `runWorkflow(name, input)` 函数（`api.js:203`），且已封装 `localControlHeaders()`。RobotPanel 重复造轮子。
- **影响**：双份 Token 解析逻辑；`api.js` 升级时 RobotPanel 不受益；违反 DRY。
- **分类**：原有问题（F-020 子项）
- **严重级别**：LOW

#### F-046: alert() 用于用户错误反馈，不符合工业 IDE 体验
- **位置**：
  - `src/components/LadderGenerator.jsx:24` `catch (err) { alert('导出失败: ' + err.message) }`
  - `src/components/ChatArea.jsx:82` `alert('导出失败: ' + err.message)`
- **现象**：用浏览器原生 `alert()` 弹窗显示导出失败。项目已有 `ui/ConfirmDialog` 与 `ErrorState` 组件，未复用。
- **影响**：阻塞 UI；与 VS Code 风格工业 IDE 的非阻塞 toast/inline 错误提示不符；在 Electron 中可能显示成原生对话框。
- **分类**：原有问题
- **严重级别**：LOW

#### F-048: ChatArea 文件 734 行接近阈值
- **位置**：`src/components/ChatArea.jsx`（734 行）
- **现象**：包含 13 个消息组件（`LadderResult`/`PlaceholderMessage`/`CodeMessage`/`IoTableMessage`/`VariablesMessage`/`TaskProgressMessage`/`ToolCallMessage`/`FileMessage`/`ExportResultMessage`/`CitationMessage`/`WarningMessage`/`ErrorMessage`/`MessageBlock`）+ `ChatInput` + 主组件 + 2 个工具函数。
- **影响**：未到 800 行但接近；建议拆分 `components/messages/` 子目录。
- **分类**：Batch 6 + 收尾批次累积
- **严重级别**：LOW

#### F-049: InspectorPanel 文件 538 行且包含 9 个 Inspector 子组件
- **位置**：`src/layout/InspectorPanel.jsx`（538 行）
- **现象**：单文件定义 9 个 Inspector 子组件（`WelcomeInspector`/`ChatInspector`/`LadderInspector`/`IoTableInspector`/`ParseInspector`/`DiagnoseInspector`/`OrchestratorInspector`/`VariablesInspector`/`SettingsInspector`）+ `PanelSection`/`KeyValue` + 主组件。D-3 收尾批次一次性塞入。
- **影响**：低于 800 行但内聚度低，每个 Inspector 内部仅是静态文案（如 `OrchestratorInspector` 全是硬编码字符串"运行中"/"Team Lead"/"Developer"），并非真实数据。建议按 tab 拆分。
- **分类**：收尾批次 D-3 引入
- **严重级别**：LOW

#### F-054: LadderInspector PLC 规范 fallback 硬编码 'S7-1200'/'V18'
- **位置**：`src/layout/InspectorPanel.jsx:255-260`
- **现象**：
  ```jsx
  <KeyValue k="PLC 类型" v={currentProject?.plc_type || 'S7-1200'} />
  <KeyValue k="TIA 版本" v={currentProject?.tia_version || 'V18'} />
  ```
  无项目时显示 `S7-1200 / V18` 而非"未选择"。
- **影响**：用户未选项目时看到"PLC 类型: S7-1200"，误以为已默认选中。其他 Inspector（如 `ChatInspector:170`）正确处理为"未选择项目，AI 助手将不携带项目上下文"，不一致。
- **分类**：收尾批次 D-3 引入
- **严重级别**：LOW

#### F-059: AppShell 关于弹窗（About）未走统一 Modal 体系
- **位置**：`src/layout/AppShell.jsx:277-302`
- **现象**：内联实现 `<div className="fixed inset-0 ...">`，未用 `ui/ConfirmDialog` 或新建 `ui/Modal`；没有 `role="dialog"`/`aria-modal`/`aria-labelledby`；`onClick={(e) => e.stopPropagation()}` 模式与 `ConfirmDialog` 重复，但没有焦点锁定与 Esc 关闭。
- **影响**：可访问性差；与 ConfirmDialog 模式不一致；增加 A11y 审查负担。
- **分类**：原有问题
- **严重级别**：LOW

#### F-060: LadderVisualizer 全部用 inline style，未走设计系统
- **位置**：`src/components/LadderVisualizer.jsx:10-23`（COLORS 常量）+ 全文 `style={{ ... }}`
- **现象**：所有颜色/边框/字体硬编码在 `COLORS` 常量与 inline style 中，如 `'#4fc3f7'`、`'#1a1a2e'`、`'#333'`，未引用 `tokens.css` 的 `var(--color-status-ok)` 等。
- **影响**：与 `tailwind.config.js` 设计 token 体系脱节；主题切换（如未来支持浅色主题）无法生效；与 `PrimarySidebar`/`BottomPanel` 等用 Tailwind 类的组件风格不一致。
- **分类**：原有问题
- **严重级别**：LOW

#### F-061: OrchestratorPanel 工作流列表与工具列表用 `key={name}` 但名称可能重复
- **位置**：`src/components/OrchestratorPanel.jsx:412` `<div key={i}>`、`OrchestratorPanel.jsx:515` `<div key={name}>`、`OrchestratorPanel.jsx:528` `<div key={name}>`
- **现象**：步骤列表用 `key={i}` 数组索引；工作流列表与工具列表用 `key={name}`。F-040 已修复 ChatArea 数组索引 key，但 OrchestratorPanel 步骤列表仍用 `key={i}`，工作流列表名称重复时会冲突。
- **影响**：工作流编辑器中步骤重排/删除时可能复用错误 DOM（与 F-040 同类风险）。
- **分类**：原有问题
- **严重级别**：LOW

#### F-063: MainWorkspace 用 display:none 保持挂载，5 个工具页 useEffect 持续轮询
- **位置**：`src/layout/MainWorkspace.jsx:82-92` + `src/components/Dashboard.jsx:92-99`（`healthCheck()`/`orchestratorHealth()`）+ `GlobalStatusBar.jsx`
- **现象**：`MainWorkspace` 用 `<div style={{ display: activeTab === tab.id ? 'flex' : 'none' }}>` 保持所有 tab 挂载。`Dashboard` 在 mount 时调用 4 个 API；`GlobalStatusBar` 15s 轮询持续；`OrchestratorPanel` mount 时拉取 5 个 API，切走后保留在内存。
- **影响**：所有 tab 同时挂载，内存占用累积；对工业 IDE 可接受，但若工具页改为轮询则会问题。
- **分类**：Batch 4 设计决策
- **严重级别**：LOW

#### F-065: CreateProjectDialog 按 Enter 提交但未阻止表单默认行为
- **位置**：`src/components/CreateProjectDialog.jsx:38`
- **现象**：`onKeyDown={e => { if (e.key === 'Enter') handleSubmit() }}` — 输入框内按 Enter 调 `handleSubmit`，但未 `e.preventDefault()`，且 `handleSubmit` 直接调 `onSubmit(form)`，未走 form 提交。如果未来加 form 包裹可能引发默认提交。
- **影响**：当前无 form 包裹，无 bug；但模式不严谨。
- **分类**：原有问题
- **严重级别**：LOW

#### F-071: InspectorPanel parseContent 函数局部定义两次
- **位置**：`src/layout/InspectorPanel.jsx:268-272`（`IoTableInspector` 内）+ `InspectorPanel.jsx:417-421`（`VariablesInspector` 内）
- **现象**：两个 Inspector 各自定义相同的 `parseContent` 局部函数。与 `ChatArea.jsx:50-61` 的 `parseContent` 三份重复。
- **影响**：违反 DRY；`ChatArea` 版本更完善（处理 `typeof parsed === 'object'`），`InspectorPanel` 两份是简化版。建议提取到 `src/utils/parseContent.js`。
- **分类**：收尾批次 D-3 引入
- **严重级别**：LOW

#### F-072: ChatArea handleExport 与 LadderGenerator doExport 重复
- **位置**：`src/components/ChatArea.jsx:70-84` + `src/components/LadderGenerator.jsx:15-25`
- **现象**：两处定义几乎相同的 `handleExport`/`doExport`：调 `exportCode(...)` → `downloadFile(...)` → `catch (err) { alert('导出失败: ' + err.message) }`。
- **影响**：DRY 违反；F-046 的 alert 问题两处都需修。
- **分类**：原有问题
- **严重级别**：LOW

#### F-073: useWorkbenchHistory save 用 Date.now().toString() 作 id，并发保存可能冲突
- **位置**：`src/hooks/useWorkbenchHistory.js:17`
- **现象**：`id: Date.now().toString()` — 同一毫秒内连续 `save` 两次（如双击生成按钮）会产生相同 id。
- **影响**：React 18 自动 batching 下同一事件回调中多次 setHistory 可能用同一 id，列表 key 冲突。
- **分类**：原有问题
- **严重级别**：LOW

#### F-074: ChatArea 用户消息无 ReactMarkdown 渲染，AI 消息有，渲染不一致
- **位置**：`src/components/ChatArea.jsx:517-518`
- **现象**：`isUser ? <div className="...whitespace-pre-wrap">{msg.content}</div>` — 用户消息用纯文本渲染；AI 消息（非 ladder/code 等）走 `<ReactMarkdown>`。用户消息中的 markdown 语法（如 `**粗体**`）会原样显示。
- **影响**：用户在输入框用 markdown 语法时，自己的消息显示原样字符，AI 回复却被渲染，视觉不一致。但这也是 XSS 防护（用户输入不渲染 HTML），可接受。
- **分类**：设计决策
- **严重级别**：LOW

#### F-076: OrchestratorPanel RunDialog/TutorialModal 用 `bg-black/90` 而 ConfirmDialog 用 `modal-backdrop`
- **位置**：`src/components/OrchestratorPanel.jsx:698`、`OrchestratorPanel.jsx:737` vs `src/components/ui/ConfirmDialog.jsx:65`
- **现象**：弹窗背景遮罩类名不统一：`RunDialog`/`TutorialModal` 用 `fixed inset-0 z-50 ... bg-black/90`，`ConfirmDialog` 用 `modal-backdrop`（CSS 类）。
- **影响**：视觉不一致；z-index 管理混乱（`z-50` vs tokens.css `--z-modal-backdrop: 50`，恰好相同但语义不同）。
- **分类**：原有问题
- **严重级别**：LOW

#### F-077: ChatArea messages.map key 用 `msg.id || ${i}-${msg.role}` fallback 仍含索引
- **位置**：`src/components/ChatArea.jsx:704`
- **现象**：`<MessageBlock key={msg.id || \`${i}-${msg.role}\`} msg={msg} />` — F-040 修复加了 `msg.id` 优先，但 fallback 仍用 `${i}-${msg.role}`，索引 `i` 仍参与。`useConversation.js` 所有消息分支都已加 `id: nextMsgId()`，理论上 fallback 不会触发，但 `handleSwitchConversation:63` 也加了 id，fallback 仍是死代码。
- **影响**：实际不会触发，但代码读起来像"索引 key 仍可能"，误导后来者。
- **分类**：收尾批次 B-3 修复不彻底
- **严重级别**：LOW

#### F-079: BottomPanel useEffect scrollIntoView 在 filteredLogs 变化时强制滚动
- **位置**：`src/layout/BottomPanel.jsx:64-68`
- **现象**：
  ```js
  useEffect(() => {
    if (!collapsed) {
      endRef.current?.scrollIntoView?.({ behavior: 'smooth' })
    }
  }, [filteredLogs, collapsed])
  ```
  与 F-078 同类问题：日志面板切 tab 或新日志到达时强制滚到底，打断用户回看。
- **分类**：收尾批次 D-2 引入
- **严重级别**：LOW

#### F-081: InspectorPanel ChatInspector ragSources 取值边缘场景
- **位置**：`src/layout/InspectorPanel.jsx:148` + `src/hooks/useConversation.js:155`
- **现象**：`useConversation` 的 `onDone` 回调把 `rag_sources: data?.rag_sources` 写到最后一条消息；`onError` 路径不写。`ChatInspector` 取 `lastAiMsg?.rag_sources`。如果 SSE 出错后走非流式 fallback（`useConversation.js:226`），`updated[updated.length - 1] = { ..., rag_sources: data.rag_sources, ... }` 正确写入。但如果 fallback 也失败（`useConversation.js:241`），错误消息无 `rag_sources`，Inspector 显示"当前对话未引用知识库"——技术上正确但可能误导用户以为没出错。
- **影响**：边缘场景，Inspector 显示与实际错误状态不匹配。
- **分类**：收尾批次 D-3 引入
- **严重级别**：LOW

#### F-082: 缺少 ErrorBoundary 包裹 OrchestratorPanel/RobotPanel 等业务组件
- **位置**：`src/layout/AppShell.jsx:157` ErrorBoundary 包裹整个 LayoutContext.Provider，但内部业务组件崩溃会触发整 App 错误页
- **现象**：ErrorBoundary 是顶层一个，OrchestratorPanel/RobotPanel/ChatArea 任一崩溃都整页重置，丢失其他 tab 状态。
- **影响**：单组件崩溃影响全局；工业 IDE 期望单 tab 崩溃不影响其他 tab。
- **分类**：原有问题
- **严重级别**：LOW

#### F-083: PrimarySidebar SYSTEM_ITEMS 中 robot 与 orchestrator 用同一 Cpu 图标
- **位置**：`src/layout/PrimarySidebar.jsx:117-119`
- **现象**：`{ id: 'orchestrator', label: '编排管理', icon: Cpu }` 和 `{ id: 'robot', label: '机器人', icon: Cpu }` 用同一个 `Cpu` 图标。
- **影响**：用户视觉混淆，无法通过图标区分。
- **分类**：Batch 4 迁移引入
- **严重级别**：LOW

#### F-084: MenuBar 编辑菜单 5 项全部 disabled，仍占菜单空间
- **位置**：`src/layout/MenuBar.jsx:24-32`
- **现象**：F-014 已记录，至今未移除或实现。`编辑` 菜单 5 项（撤销/重做/剪切/复制/粘贴）全部 `disabled: true`。
- **影响**：用户点击"编辑"看到全灰菜单，无功能但占空间。
- **分类**：F-014 残留
- **严重级别**：LOW

#### F-085: 8 个 Ctrl 快捷键标注未实现（F-013 残留）
- **位置**：`src/layout/MenuBar.jsx:17-31`
- **现象**：菜单标注 `Ctrl+N/Z/Y/X/C/V` 6 个快捷键，`AppShell.jsx:128-137` 只实现 `Ctrl+B/J/\`` 3 个。F-013 已记录，未修。
- **影响**：用户期望 Ctrl+N 新建项目，实际无效。
- **分类**：F-013 残留
- **严重级别**：LOW

#### F-088: OrchestratorPanel useEffect 不清理，卸载后 setState 可能告警
- **位置**：`src/components/OrchestratorPanel.jsx:206`
- **现象**：`useEffect(() => { fetchAll() }, [fetchAll])` — `fetchAll` 是 useCallback 依赖空数组，稳定。但 fetchAll 内多个 `setWorkflows`/`setTools` 等 setState 在组件卸载后仍可能被调用（fetch 未中止）。
- **影响**：React 18 不再告警卸载后 setState，但仍是反模式；切走 tab 后 OrchestratorPanel 不卸载（F-063），影响小。
- **分类**：原有问题
- **严重级别**：LOW

#### F-089: ChatArea textarea 自动调整高度未实现
- **位置**：`src/components/ChatArea.jsx:604-612`
- **现象**：textarea `rows={1}` + `min-h-[38px] max-h-32`，但无 `onInput` 自动调整高度逻辑。用户输入多行时 textarea 不会长高，需手动拖拽。
- **影响**：UX 细节，VS Code 风格输入区通常会自动长高到 max-h。
- **分类**：Batch 6 引入
- **严重级别**：LOW

---

## 三、测试覆盖评估

### 当前测试基线
- **4 个测试文件，52 个测试用例**（与 `progress.md:58` 一致）：
  - `src/components/LadderGenerator.test.jsx`：2 用例（pipeline 成功 + 失败）
  - `src/components/ui/ui.test.jsx`：38 用例（23 个 UI 基础组件 + ToolStatusBar 3 用例）
  - `src/platform/runtime.test.js`：7 用例（isElectron/isWeb/getRuntimeMode + API_BASE/API_DOCS_URL）
  - `src/platform/safetyLevels.test.js`：5 用例（4 等级定义 + 9 字段 + 4 按钮）

### 完全无测试的关键组件（按风险排序）

| 组件 | 行数 | 关键性 | 风险 |
|------|------|--------|------|
| `src/hooks/useConversation.js` | 272 | **极高** — SSE 流式 + AbortController + 消息状态机 + 4 个 catch 分支 | F-039/F-040/F-042 等多处修复均无回归测试 |
| `src/layout/AppShell.jsx` | 307 | **极高** — 6 个 hooks 组合 + 5 个 Modal + 键盘快捷键 + LayoutContext | F-029~F-032 修复无回归测试 |
| `src/hooks/useTabs.js` | 41 | **高** — F-033/F-037 stale closure 修复无测试 | 快速连点 closeTab 行为无验证 |
| `src/layout/GlobalStatusBar.jsx` | 260 | **高** — D-4 真实 API 接入 + 安全等级切换 + localStorage | 安全等级切换无测试 |
| `src/components/ChatArea.jsx` | 734 | **高** — 13 种消息类型分发 + SSE 状态 + F-039/F-040/F-041 修复 | 收尾批次 D-1 新增 7 个消息组件无测试 |
| `src/layout/InspectorPanel.jsx` | 538 | **高** — 9 种 Inspector + D-3 新增 | F-050/F-051/F-067 假数据问题无测试拦截 |
| `src/layout/BottomPanel.jsx` | 124 | 中 — 7 Tab 过滤逻辑 | D-2 过滤正则无测试 |
| `src/layout/PrimarySidebar.jsx` | 367 | 中 — 4 分组 + 删除对话 inline 确认 | F-066 删除流程无测试 |
| `src/layout/MainWorkspace.jsx` | 95 | 中 — display:none 挂载策略 | 无测试 |
| `src/layout/WorkspaceTabs.jsx` | 37 | 中 — tab 关闭交互 | 无测试 |
| `src/layout/MenuBar.jsx` | 88 | 中 — 菜单配置 | 无测试 |
| `src/layout/TopBar.jsx` | 27 | 低 — 容器 | 无测试 |
| `src/components/RobotPanel.jsx` | 469 | 中 — 急停/自动循环/4 模式缺失 | F-019 无测试拦截 |
| `src/components/OrchestratorPanel.jsx` | 870 | 中 — 工作流编辑器 + RunDialog | 无测试 |
| `src/components/SettingsPanel.jsx` | 236 | 中 — API Key 表单 + 测试连接 | F-056 reply 渲染无测试 |
| `src/components/Dashboard.jsx` | 279 | 中 — 5 区域总览 | F-067/F-068 硬编码无测试 |
| 其他 17 个组件 | — | 低 | 无测试 |

### 覆盖率估算
- **有测试的文件**：4/33（约 12% 文件覆盖）
- **关键路径覆盖**：仅 LadderGenerator pipeline + UI 基础组件 + platform runtime + safetyLevels 常量
- **未覆盖关键路径**：SSE 流式对话、AbortController 停止、消息状态机、Modal 焦点锁定、安全等级切换、Inspector 假数据检测、BottomPanel 日志过滤、useTabs closeTab、LayoutContext、5 个工具页状态机
- **估算行覆盖率**：约 10-15%（远低于 CLAUDE.md 要求的 80%）
- **E2E 覆盖**：0%（无 Playwright spec 文件）

### 测试质量评估
- 现有 52 用例质量良好：ui.test.jsx 覆盖 23 个基础组件的主要交互；LadderGenerator.test.jsx 正确 mock 了 API 与子组件；safetyLevels.test.jsx 验证了 9 字段 + 4 按钮常量。
- 但关键业务逻辑（SSE/AbortController/状态机/Modal 焦点）完全无测试保护，F-039/F-040/F-041 等修复均靠手动验证，无回归测试。

---

## 四、构建体积评估

### 当前构建（progress.md 宣称数据）
- JS 472.54 KB（未 gzip）/ gzip 136.86 KB
- CSS 33.10 KB
- 1999 modules

### App 页面 gzip 预算评估（CLAUDE.md `web/performance.md`：App 页面 < 300 KB gzip）
- **136.86 KB gzip**，远低于 300 KB 预算（仅占 45.6%）。
- 预算充裕，可继续添加功能而无需拆包。

### 体积偏大模块分析（基于源码行数 + 依赖推断）
1. **react-markdown**（`package.json:55` `^9.0.1`）— 在 `ChatArea.jsx` + `CodeExplainer.jsx` + `IoTableGenerator.jsx` + `FaultDiagnosis.jsx` + `VariableAnalyzer.jsx` 5 处使用，是 bundle 最大的第三方依赖之一。
2. **lucide-react**（`package.json:52` `^1.20.0`）— 大量图标按需 import，tree-shaking 应该有效，但 `OrchestratorPanel.jsx:1-8` 一次 import 20+ 图标。
3. **OrchestratorPanel.jsx**（870 行）+ **ChatArea.jsx**（734 行）+ **InspectorPanel.jsx**（538 行）+ **RobotPanel.jsx**（469 行）+ **Dashboard.jsx**（279 行）— 5 个大组件占 src 总行数 8838 中的 2890 行（32.7%）。
4. **react/react-dom** — 标准 ~40 KB gzip。
5. **5 个工具页 streamChat 重复 prompt 常量** — 每个 prompt 1-2 KB，5 个合计 ~7 KB，可考虑后端化。

### 优化建议（不实施，仅记录）
- **Code Splitting**：5 个工具页（LadderGenerator/CodeExplainer/IoTableGenerator/FaultDiagnosis/VariableAnalyzer）+ OrchestratorPanel + RobotPanel + SettingsPanel 可拆为 `React.lazy` 动态 import，减少首屏 JS。当前 1999 modules 全打进一个 chunk。
- **react-markdown 按需**：5 个工具页的 ReactMarkdown 可考虑共用一个 lazy 加载的 Markdown 渲染器。
- **当前体积在预算内**，无需立即优化。

---

## 五、总体评分与建议

### 评分分解

| 维度 | 得分 | 满分 | 说明 |
|------|------|------|------|
| 安全性 | 6 | 10 | CSP `connect-src https:` 通配未收紧 + `script-src 'unsafe-inline'` 未改 nonce + SettingsPanel `reply` 渲染泄露 + ErrorBoundary console.error 不脱敏 + 9 处静默 catch |
| 代码质量 | 6 | 10 | OrchestratorPanel 870 行超阈值 + ChatArea 734 行 + 3 份 parseContent 重复 + 2 份 export 重复 + 9 个 catch {} 静默 + 数组索引 key 残留 |
| 性能 | 7 | 10 | 136 KB gzip 在预算内 + layoutContextValue 已 memo + handleSend 依赖 messages 导致重渲染 + useEffect 强制滚动打断回看 + 5 个工具页 streamChat 无 signal |
| 可访问性 | 6 | 10 | useFocusTrap 仅 5 个弹窗，RunDialog/TutorialModal/About 漏掉 + PrimarySidebar 删除对话 inline 确认无 focus trap + ARIA 标注不完整（OrchestratorPanel RunDialog 无 role=dialog） |
| 一致性 | 5 | 10 | 4 处直接 fetch 绕过 api.js + Dashboard 硬编码状态与 GlobalStatusBar 真实 API 矛盾 + InspectorPanel 9 个 Inspector 中 5 个含硬编码假数据 + 弹窗背景遮罩类名不统一 |
| 测试覆盖 | 3 | 10 | 52 用例 / 4 文件 / ~12% 文件覆盖 / 0% E2E / 关键路径（SSE/AbortController/Modal 焦点/状态机）完全无测试 |
| 主计划合规性 | 7 | 10 | 13 种消息类型已实现 + 4 种安全等级已定义 + VS Code 风格 + 9 Batch 重构完成 + 但 F-019 机器人 4 模式/F-037 useTabs 根治/ToolStatusBar 接入/CSP 收紧/E2E 均未完成 |
| 构建体积 | 9 | 10 | 136.86 KB gzip 在 300 KB 预算内（45.6%）+ 1999 modules 单 chunk 可优化但非必需 |

**加权总分**：6.8 / 10

### 结论：**CONDITIONAL PASS — 需修复后再合并生产**

---

## 六、P0-P7 修复优先级

### P0 — 修复 6 个 HIGH 问题（预计 1-2 天）
- **F-050/F-051/F-067/F-068**：删除 Inspector/Dashboard 中所有硬编码"状态/校验/地址范围/安全模式"假数据，改为接真实 API 或明确标注"待接入"
- **F-042**：useConversation fallback fetch 加 localControlHeaders
- **F-056**：SettingsPanel testResult.reply 仅 dev 显示或脱敏

### P1 — 补关键路径单元测试（预计 2-3 天）
- `useConversation.test.js`：SSE 流式 + AbortController + fallback + F-039/F-040/F-041 回归
- `useTabs.test.js`：closeTab stale closure + StrictMode 双调用
- `useFocusTrap.test.js`：Tab 循环 + 焦点恢复
- `AppShell.test.js`：LayoutContext + 键盘快捷键 + Modal 注册
- `GlobalStatusBar.test.js`：安全等级切换 + localStorage 持久化
- 目标：关键 hooks 与 layout 组件覆盖率 > 60%

### P2 — 修复 F-019 机器人 4 模式 + F-037 useTabs 根治（预计 1-2 天）
- RobotPanel 加 mode 切换（演示/仿真/只读/真实控制），真实控制模式接 SAFETY_LEVELS + ConfirmDialog
- useTabs 合并 `tabs` + `activeTab` 为单一 state object，消除 updater 内副作用

### P3 — ToolStatusBar 接入 5 个工具页面 + 状态机改造（预计 2-3 天）
- 5 个工具页 import ToolStatusBar，替换自实现的 loading 文案
- 状态从 `loading: boolean` 扩展为 `status: idle/inputting/validation_failed/running/success/failed/partial/no_result/offline/model_unavailable`

### P4 — 附件上传按钮实现真实文件选择（预计 0.5 天）
- ChatArea 附件按钮接 `<input type="file">` + 调 `uploadDocument()` API

### P5 — CSP 收紧 + ErrorBoundary 脱敏 + catch {} 加日志（预计 0.5 天）
- `index.html` `connect-src https:` 改为具体域名（生产环境通过环境变量注入）
- `script-src 'unsafe-inline'` 改 nonce-based（Vite 生产构建支持）
- ErrorBoundary console.error 改为脱敏后的 message + stack 第一行
- 9 处 `catch {}` 至少加 `console.warn` 或 addLog

### P6 — 响应式 4 尺寸截图回归 + E2E + Lighthouse（预计 2-3 天）
- 写 `scripts/responsive-screenshot.mjs`：1366/1600/1920/2560 四尺寸 Playwright 截图
- 写 `playwright.config.js` + 5 个关键流程 E2E spec（新建项目/对话/生成梯形图/导出/切换 tab）
- 跑 Lighthouse 生成性能报告

### P7 — 文件拆分（预计 1-2 天）
- `OrchestratorPanel.jsx` 870 行拆为 `OrchestratorPanel/` 目录（主组件 + RunDialog + TutorialModal + ToolGroup + helpers）
- `ChatArea.jsx` 734 行拆为 `components/messages/` 子目录（13 个消息组件各自独立文件）
- `InspectorPanel.jsx` 538 行拆为 `layout/inspectors/` 子目录（9 个 Inspector 各自独立文件）

**总计**：8-13 天可达 PASS 标准。

---

## 七、关键产物路径

- 状态文件：`ai-plc-assistant/frontend/{task_plan,progress,findings,decisions}.md`
- 本次复审报告：`ai-plc-assistant/frontend/audit-2026-07-20.md`（本文件）
- 主计划：`ai_plc_frontend_cc_pack/docs/frontend/frontend-redesign-master-plan.md`
- 审核文档：`docs/frontend/current-{ui-audit,pages,components,api-usage,navigation,web-compatibility}.md`
- 截图基线：`docs/frontend/screenshots/before/`（28 张，Batch 1）
- 截图验证：`docs/frontend/screenshots/batch{2,3,4,5,6,7}-verify/`（8/8/8/8/8/5 张）
- 截图收尾：`docs/frontend/screenshots/followup-verify/`（8 张）

---

## 八、总结

前端 9 Batch 重构 + 收尾批次完成了从 187 行单文件 App.jsx 到 33 个源文件 / 8838 行 / 9 个 layout 组件 / 23 个 UI 基础组件 / 13 种消息类型 / 4 种安全等级 / VS Code 风格工业 IDE 工作台的架构演进，构建体积 136.86 KB gzip 在 300 KB 预算内，主计划合规性 70%。

**但存在 6 个 HIGH 级别问题阻断生产部署**：
- InspectorPanel 与 Dashboard 中 5 处硬编码假数据（F-050/F-051/F-067/F-068）违反项目"不伪造"安全原则，在工业场景下"假校验通过/假状态运行中"可能误导操作员造成设备损坏
- useConversation fallback fetch 缺 Token（F-042）导致 SSE 失败后 fallback 必失败
- SettingsPanel reply 渲染泄露（F-056）

测试覆盖仅 12% 文件 / 0% E2E，关键路径无回归保护。

**建议**：先修 6 个 HIGH 问题（1-2 天），再补关键路径测试（2-3 天），然后处理 F-019/F-037/ToolStatusBar 接入（3-5 天），最后做响应式/E2E/性能（2-3 天）。总计 8-13 天可达 PASS 标准。
