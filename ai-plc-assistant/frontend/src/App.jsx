import AppShell from './layout/AppShell'

/**
 * App — 根组件（仅渲染 AppShell）
 *
 * Batch 4：应用外壳已拆分到 src/layout/。
 * 此文件仅作为入口，实际布局逻辑在 AppShell.jsx。
 */
export default function App() {
  return <AppShell />
}
