我正在做一个 AI 接入 PLC 的项目（西门子 S7-1500 + TIA Portal + PLCSIM Advanced V5.0），目前已用 pythonnet 封装了 PLCSIM Advanced 的 .NET API，卡在最后一步：

PLCSIM Advanced 实例是空壳 CPU，需要硬件配置才能 Run()，但硬件配置必须通过 TIA Portal GUI 首次下载（download）才能写入。Openness API 的 DownloadProvider 不支持首次下载（西门子官方限制）。

有没有什么办法绕过"首次下载必须 TIA Portal GUI 手动操作"这个限制？
比如：
1. 用 PLCSIM 其他 API 直接加载硬件配置？
2. 有没有类似 virt-sandbox / 预置镜像的方式？
3. 能否通过直接操作 PLCSIM 的磁盘文件来注入配置？
4. 如果把 TIA 项目导出为 AML 或 XML，然后用 Openness 导入的方式能工作吗？
5. 完全替代方案：用 OpenPLC 仿真 + S7 通信模拟来绕过这个问题？

请给具体的建议，尤其针对 PLCSIM Advanced V5.0。