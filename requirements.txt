# ═══════════════════════════════════════════════════════════════
# AI-PLC 统一 Python 依赖（直接依赖 + 最低版本约束）
# ═══════════════════════════════════════════════════════════════
# 安装: python -m pip install -r requirements.txt
#
# 设计说明:
#   - 本文件只列直接依赖，不再是 pip freeze 全量快照；传递依赖交给 pip 解析。
#   - 最低版本来自 2026-07 在干净环境实际跑通全部离线测试（682 passed）
#     的组合；各子服务（如 edge-gateway）可有自己的 requirements。
#   - backend/requirements.txt 与本文件已对齐，不可单独降级其中的
#     fastapi/starlette/httpx/pydantic，否则与 fastmcp/mcp 冲突。

# ── MCP / 编排层 ──
mcp>=1.28
fastmcp>=3.3
PyYAML>=6.0
pydantic>=2.10
python-dotenv>=1.0
requests>=2.32
keyring>=25.0
websockets>=14.0
websocket-client>=1.8

# ── PLC 协议适配 ──
python-snap7>=2.0        # S7 通信（随 wheel 附带原生库）
pymodbus>=3.6            # Modbus TCP
asyncua>=1.1             # OPC UA
influxdb-client>=1.43    # 机器人监控时序数据（robot-mcp 可选）

# ── AI PLC Assistant 后端 ──
fastapi>=0.139
uvicorn>=0.30
sse-starlette>=2.2
pydantic-settings>=2.4
httpx>=0.28
openai>=1.50
anthropic>=0.60
chromadb>=0.5.5
fastembed>=0.3
PyMuPDF>=1.24
python-docx>=1.1
python-multipart>=0.0.9

# ── Windows 桌面/凭据集成 ──
pywin32>=306; platform_system == "Windows"

# ── 开发与测试 ──
pytest>=8.2
pytest-asyncio>=0.23
